name: Qucik Wails App Build Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.24'

    - name: Build wails
      uses: dAppServer/wails-build-action@v2.9.0
      id: build
      with:
        build-platform: linux/amd64
        package: false
        go-version: '1.24'
